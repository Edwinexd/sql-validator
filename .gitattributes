# Normalize line endings: all text files are stored and checked out as LF.
* text=auto eol=lf

# Binary files: never touch line endings.
*.png    binary
*.ico    binary
*.sqlite3 binary
*.enc    binary
